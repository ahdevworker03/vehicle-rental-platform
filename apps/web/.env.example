# Base URL of the API origin (without the /api path suffix).
# The generated client already prefixes request paths with /api.
# Leave empty to use the same origin as the application (production / proxied).
# Example for local development:
# VITE_API_URL=http://localhost:3000
