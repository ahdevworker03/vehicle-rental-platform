// Central icon lookup so nav config (data) can stay decoupled from lucide imports.
import {
  LayoutDashboard,
  Car,
  Users,
  FileSignature,
  Wrench,
  Receipt,
  Wallet,
  ListChecks,
  BarChart3,
  FileBarChart2,
  Settings,
  type LucideIcon,
} from "lucide-react";

export const iconMap: Record<string, LucideIcon> = {
  LayoutDashboard,
  Car,
  Users,
  FileSignature,
  Wrench,
  Receipt,
  Wallet,
  ListChecks,
  BarChart3,
  FileBarChart2,
  Settings,
};
