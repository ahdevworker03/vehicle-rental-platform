# Milestone 5.8 - Product Frontend Readiness

## Goal

Upgrade `apps/web` into a polished, responsive, Arabic-first SaaS product before Milestone 6. The work improves tenant-facing UI/UX, frontend structure, generated API-client integration, and missing screens for approved backend capabilities while preserving established business behavior and contracts.

The implementation baseline is:

- `docs/architecture/12-ui-design-system.md`
- `docs/audits/frontend-work/ui-audit-result.md`

The current `apps/web` source is the practical frontend reference. `docs/architecture/03-frontend-architecture.md` describes the older mock prototype and must not override current source or the updated audit/design-system documents.

## Rule

**Rule:** No step may be skipped. Every step must be:

1. Implemented.
2. Reviewed by the AI agent.
3. Reviewed by the product owner.
4. Tested.
5. Committed.

Only after all acceptance criteria are satisfied may the next step begin.

- Existing backend behavior and API contracts must be preserved unless a verified frontend integration bug requires a narrowly scoped fix.
- Shared layout and design-system work must precede screen rebuild work.
- Every implementation phase requires automated checks and responsive/manual review.
- Do not introduce Version 2-excluded features.

## Scope

- Practical feature-based frontend structure and shared infrastructure cleanup.
- Responsive app shell, navigation, dashboard, core business, operations, insights, auth, account, and organization UI.
- API-consistent loading, unavailable, zero, empty, error, submitting, and permission states.
- Existing API-backed support for document expiry, recurring tasks, maintenance schedules, invitations, password reset, organization profile, and organization lifecycle status.
- Arabic RTL, mixed-direction value, accessibility, and responsive quality at 375px, 768px, 1024px, 1440px, and 1920px.

## Non-Goals

- Backend/database correctness remediation, new backend features, or API-contract changes unless a verified frontend integration gap requires a narrowly scoped fix.
- Production deployment, offline synchronization, real password-reset delivery, secure `PLATFORM_OWNER` bootstrap/runbook, performance/security hardening, or Milestone 6 documentation work.
- Full Platform Admin Dashboard, billing/subscriptions, Whish/QR payments, notification delivery, background schedulers, maintenance automation, vehicle sales, customer portal, online reservations, accounting integrations, support CRM, or tenant impersonation.
- A platform-owner route/entry point may be reserved only as a boundary for Milestone 6. Tenant-facing navigation must not expose platform controls.

## Current Baseline

- `App.tsx` uses Wouter, `AppShell`, `ProtectedRoute`, `QueryProvider`, and `AuthProvider`; only `/login` is currently public.
- Existing pages cover dashboard, vehicles, customers, rentals, maintenance, expenses, tasks, analytics, and reports, with many add/edit/detail routes.
- The application has shared layout, domain, and shadcn-style UI components plus generated TanStack Query client hooks.
- Legacy hooks under `features/vehicles`, `features/customers`, and `features/rentals` still read local data, while newer screens use `@workspace/api-client-react`. This mixed source is a P0 frontend truthfulness risk.
- Registration, password reset, invitation acceptance, organization profile, maintenance schedule, and organization lifecycle APIs are generated but not fully represented as frontend routes/workflows.

## Dependencies

|  Step | Depends on | Reason                                                                  |
| ----: | ---------- | ----------------------------------------------------------------------- |
|    40 | Complete   | Audit and baseline define approved direction                            |
|    41 | 40         | Structure plan must follow the approved UI/API findings                 |
|    42 | 41         | Shared boundaries and infrastructure follow the approved migration plan |
|    43 | 42         | Shell uses shared layout, state, and feedback primitives                |
| 44-45 | 42-43      | Dashboard requires consistent data and the approved shell               |
| 46-48 | 42-43      | Core screens use the shared patterns and navigation                     |
| 49-51 | 42-43      | Operations screens use the shared patterns and API boundary             |
| 52-53 | 42-43      | Insights screens depend on consistent period/state treatment            |
| 54-55 | 42-43      | Auth/account routes use shared session, form, and feedback rules        |
|    56 | 43-55      | Final regression covers all completed frontend work                     |

## Shared Verification

For every step that changes frontend code, run the relevant checks:

```sh
pnpm --filter @workspace/web typecheck
pnpm --filter @workspace/web test
pnpm --filter @workspace/web build
pnpm lint
git diff --check
```

When generated API usage or integration changes, run the relevant generated-client/API checks before claiming contract compatibility. Do not edit generated files manually.

Manual review for visual steps includes 375px, 768px, 1024px, 1440px, and 1920px, real Arabic strings, mixed-direction numbers/dates/identifiers, keyboard navigation, visible focus, dialog/sheet focus handling, and reduced-motion behavior.

Product review questions:

- Does this help rental businesses work faster?
- Is the primary action, current status, and next valid action clear?
- Does desktop space feel useful rather than empty?
- Does mobile remain efficient?
- Are Arabic labels readable and natural?
- Are loading, empty, unavailable, zero, error, and permission states truthful?

---

## Step 40 — Frontend Audit and Design-System Baseline

### Status

Complete.

### Must Read

- `docs/audits/frontend-work/ui-audit-result.md`
- `docs/architecture/12-ui-design-system.md`

### Objectives

Establish the approved UI direction, risks, responsive rules, API-state rules, and implementation constraints.

**Scope:** Documentation only. The audit and design-system baseline are the mandatory sources for every later implementation step.

**Excluded:** Frontend code, API, backend, and screen rebuild work.

### Deliverables

- Completed audit and concise design-system baseline.

### Acceptance Criteria

- Both documents exist, identify the mixed-data risk and missing frontend capabilities, and define Arabic RTL, responsive, accessibility, navigation, feedback, and platform-boundary rules.

### Verification

- Documentation review.
- `git diff --check`.

---

## Step 41 — Feature-Based Frontend Structure Plan

### Must Read

- Step 40 outputs.
- `apps/web/src/App.tsx`, `pages/`, `features/`, `components/`, `components/layout/`, `components/ui/`, `providers/`, `hooks/`, `lib/`, `data/`, and generated client usage.

### Objectives

Define a practical migration plan from the current flat/mixed frontend structure to a feature-based architecture without rewriting the application.

The plan must classify current frontend files by ownership and define where each file should live after the refactor.

**Scope:** Inspect the current frontend structure and classify relevant files into:

- app-owned
- feature-owned
- shared UI
- shared layout/infrastructure
- shared utilities/hooks
- transitional or obsolete

The plan must explicitly review these crowded areas:

- `apps/web/src/pages`
- `apps/web/src/components`
- `apps/web/src/components/layout`
- `apps/web/src/components/ui`
- `apps/web/src/lib`
- `apps/web/src/hooks`
- `apps/web/src/providers`
- `apps/web/src/features`
- `apps/web/src/data`

Domain-specific pages, components, hooks, selectors, schemas, helpers, and API adapters should move under their owning feature where appropriate.

Shared shadcn-style primitives and genuinely reusable UI components should remain shared and must not be duplicated across features.

Shared layout code may be grouped by responsibility, for example:

```text
components/
  layout/
    shell/
    navigation/
    page/
```

The target frontend direction should be approximately:

```text
src/
  app/
    routes/
    providers/
    App.tsx

  features/
    auth/
    dashboard/
    vehicles/
    customers/
    rentals/
    maintenance/
    expenses/
    tasks/
    analytics/
    reports/
    organization/

  components/
    layout/
    ui/

  hooks/
  lib/
  data/
```

The exact structure must be based on repository inspection rather than forcing empty or unnecessary folders.

For every route-level page and domain-specific component/helper, define its intended target owner.

Also inspect file size and responsibility boundaries.

Files larger than roughly **300-400 lines** should be reviewed for splitting when they contain multiple responsibilities.

Reduce oversized files by extracting cohesive concerns such as:

- page sections
- domain components
- form schemas
- form sections
- hooks
- selectors
- helpers
- API adapters
- configuration
- route definitions

Do not split files only to satisfy a line-count target. A large file may remain intact when it represents one cohesive responsibility and splitting it would make the code harder to understand.

When splitting an oversized file:

- preserve existing behavior
- preserve public component/hook contracts where practical
- avoid circular dependencies
- avoid creating tiny one-use files without a clear responsibility
- keep related code close to its owning feature
- update tests/imports as part of the later migration step
- do not change business logic merely to reduce file size

The plan must also define:

- import boundaries between `app`, `features`, and shared code
- migration order
- local/mock data retirement order
- which top-level folders should disappear, shrink, or remain shared
- which oversized files need decomposition during the migration
- which files should remain unchanged because they are already correctly owned

**Excluded:** Moving files, changing imports, splitting files, visual redesign, changing routes, replacing frameworks/libraries, changing API contracts, or changing business behavior in this planning step.

### Deliverables

- Exact target frontend folder structure based on current repository ownership.
- File ownership/migration map for current pages, components, layout, UI, hooks, providers, lib, and data.
- List of domain-specific files that should move into features.
- List of shared files that should remain shared.
- List of oversized files above roughly 300-400 lines that should be decomposed, with the intended responsibility split.
- Approved migration order.
- Import-boundary rules.
- Explicit local/mock-data retirement order.

### Acceptance Criteria

- Every current route-level page has an explicit target owner.
- Every domain-specific component/helper reviewed has an explicit feature or shared owner.
- Shared shadcn/UI primitives remain shared rather than being duplicated inside features.
- `pages/`, `components/layout/`, `components/ui/`, `lib/`, and other crowded folders are evaluated individually instead of being reorganized blindly.
- The plan identifies which crowded folders will disappear, shrink, or be reorganized after migration.
- Oversized files above roughly 300-400 lines are identified and only scheduled for splitting where multiple responsibilities justify it.
- No file is split purely to meet an arbitrary line-count requirement.
- The proposed structure preserves Wouter, Tailwind, existing shadcn-style primitives, generated API hooks, current routes, and existing business behavior.
- The migration plan avoids circular dependencies and unnecessary abstractions.
- Dashboard, vehicle, customer, and rental screens have a clear path away from mixed local/API data sources.

### Verification

- Documentation review and repository ownership review against the current `apps/web/src` tree.
- AI/product-owner approval.
- `git diff --check`.

---

## Step 42 — Frontend Architecture Migration & Shared Infrastructure

### Must Read

Step 41 plan, `QueryProvider`, `AuthProvider`, API configuration/error utilities, shared layout components, feedback components, and affected generated hooks.

### Objectives

Establish reusable frontend boundaries before page rebuilds.

**Scope:** Apply the approved structure incrementally; standardize generated API-hook usage, query invalidation ownership, formatting, Arabic API-error mapping, page/container primitives, and loading/empty/error/permission feedback states. Preserve routes and business behavior.

**Excluded:** Screen-specific visual rebuilds, new API contracts, local-data removal beyond approved affected boundaries, and session-security redesign beyond frontend refresh/retry behavior.

**Deliverables:** Shared infrastructure with documented import boundaries, common feedback primitives, and a migration-safe API usage pattern.

**Acceptance Criteria:** Shared layers do not import feature pages; features own domain API adapters/selectors; each migrated screen has one authoritative visible data source; user-facing errors are concise Arabic recovery messages.

**Verification:** Shared verification; focused tests for API errors, query invalidation, and feedback states; responsive smoke review of shared shell primitives.

---

## Step 43 — App Shell, Navigation, and Layout System

### Must Read

Step 40 outputs, Step 42 infrastructure, `AppShell`, `AppSidebar`, `TabletNavigationRail`, `BottomNavigation`, `NavigationDrawer`, `PageContainer`, and navigation definitions.

### Objectives

Implement one responsive navigation and layout contract before rebuilding screens.

**Scope:** Rebuild desktop sidebar, tablet navigation, mobile bottom navigation and More menu, page gutters/containers, active-route states, skip-link/main-content behavior, and account/company destination. Add tenant layout handling for suspended/cancelled organizations while preserving authentication and account-status access.

**Excluded:** Platform-admin dashboard, platform lifecycle controls in tenant navigation, notification center, or changes to authorization behavior.

**Deliverables:** Reusable shell/navigation/layout primitives and route integration guidance.

**Acceptance Criteria:** Desktop uses grouped sidebar navigation; mobile exposes dashboard, rentals, vehicles, and customers plus a clear accessible More path; account/company navigation is present; platform-only controls are absent; full-screen create flows retain a clear back/cancel path.

**Verification:** Shared verification; review all target widths; keyboard, focus, route-active, More-menu, safe-area, and suspended/cancelled-state checks.

---

## Step 44 — Dashboard Data Consistency and Period Semantics

### Must Read

Step 42 infrastructure, `DashboardPage`, dashboard selectors/hooks, generated vehicle/payment/expense/maintenance/task hooks, and report selectors.

### Objectives

Make dashboard values truthful before visual redesign.

**Scope:** Remove misleading mixed local/API display behavior where present; define authoritative sources for visible metrics and rows; replace fixed/mock period assumptions with explicit current or selected period semantics; distinguish loading, unavailable, zero, stale/refreshing, and error states.

**Excluded:** New dashboard API endpoints, new financial calculations, changing backend aggregation semantics, or visual dashboard rework beyond state clarity needed for correctness.

**Deliverables:** Data-source map, period semantics, and tested dashboard state behavior.

**Acceptance Criteria:** A displayed entity/metric cannot combine conflicting mock and API records; financial values never fall back to zero on error; period and data scope are visible; retry behavior preserves context.

**Verification:** Shared verification; tests for period/state derivation; manual checks with successful, empty, unavailable, and failing API responses.

## Step 45 — Dashboard UI Rebuild

### Must Read

Step 40 outputs, Step 43 shell, Step 44 data behavior, `DashboardMetricCard`, `SectionCard`, and feedback components.

### Objectives

Rebuild the daily operational overview using the approved design system.

**Scope:** Prioritize overdue returns, returns due today, outstanding balances, fleet availability, urgent maintenance/tasks/document expiry in the first viewport. Rebuild KPI cards, operational alerts, financial summary, recent activity, and navigation shortcuts with RTL-responsive layouts.

**Excluded:** New alerts/notification delivery, invented dashboard metrics, fixed/mock periods, or platform-only administration.

**Deliverables:** Responsive dashboard composition and tests for key states/actions.

**Acceptance Criteria:** Urgent rows have a clear destination; KPI/status treatment is consistent; financial period is explicit; actions are keyboard accessible; no state is communicated by color alone.

**Verification:** Shared verification; all target widths; product-owner review of first-viewport hierarchy and real Arabic copy.

---

## Step 46 — Vehicle Screens Rebuild

### Must Read

Step 40 outputs, vehicle routes/pages/components, media/document hooks, generated vehicle/document APIs, and vehicle user flows.

### Objectives

Make fleet management fast to scan and safe to act on.

**Scope:** Rebuild vehicle list, detail, create, and edit flows; surface photos/documents, document expiry visibility/editing, availability/status-first summary, and related maintenance/rental sections.

**Excluded:** New vehicle statuses, vehicle sales, automated expiry reminders, or document backend changes.

**Deliverables:** Consistent vehicle list/detail/form patterns and document-state UX.

**Acceptance Criteria:** Lists support the available search/filter/sort/pagination behavior or a truthful equivalent; detail leads with status and next action; expiry update uses existing APIs; uploads show loading/failure/retry states.

**Verification:** Shared verification; document/photo mutation tests; Arabic vehicle data, long identifiers, and mobile list/card review.

## Step 47 — Customer Screens Rebuild

### Must Read

Step 40 outputs, customer routes/pages/components, document hooks, generated customer APIs, and customer user flows.

### Objectives

Make customer lookup and rental readiness clear.

**Scope:** Rebuild customer list, detail, create, and edit; improve search behavior/context preservation; present license/national ID, documents and expiry, rental history, and outstanding-balance context where existing data supports it.

**Excluded:** New customer fields, customer portal, new search backend behavior, or changed archival rules.

**Deliverables:** Reusable customer list/detail/form and document patterns.

**Acceptance Criteria:** Search scope is clear; sensitive identifiers remain readable without excessive exposure; detail prioritizes active rental, balance, license validity, documents, history, and next action; destructive actions are confirmed.

**Verification:** Shared verification; tests for search/empty/error/document states; RTL truncation/wrapping and keyboard review.

## Step 48 — Rental, Contract, and Payment Screens Rebuild

### Must Read

Step 40 outputs, rental routes/pages/components, contract/payment/media hooks, availability hooks, generated rental/contract/payment APIs, and rental user flows.

### Objectives

Make the highest-risk financial workflow status-led and truthful.

**Scope:** Rebuild rental list, detail, and new-rental flow; show status timeline/next action; preserve pickup, return, extend, and cancel actions; rebuild rental-context payment section; clarify contract generation/download/print/signed-document upload states; handle no-contract states; retain financial totals visibly.

**Excluded:** Organization-wide payments route, payment gateway/Whish/QR work, contract-version redesign, reservation features, or changed rental rules.

**Deliverables:** Status-dependent rental UI, contract/signed-contract states, and rental-context payment pattern.

**Acceptance Criteria:** Total, deposit, paid, and outstanding values are visible and sourced consistently; a generated contract is not presented as signed; invalid actions are unavailable or explained; destructive/status-changing actions are confirmed.

**Verification:** Shared verification; workflow tests for availability conflict, pickup, return, extension, cancellation, payment, contract, and signed-document failure states; all target widths.

---

## Step 49 — Maintenance and Maintenance Schedule Screens

### Must Read

Step 40 outputs, maintenance routes/pages/components, generated maintenance and schedule APIs, and maintenance user flows.

### Objectives

Separate current maintenance work from planned maintenance schedules.

**Scope:** Rebuild maintenance list, detail, create, edit, and complete flows; add schedule CRUD UI using existing API behavior; distinguish record status from schedule basis, due values, and active state.

**Excluded:** Automatic maintenance-record generation, notifications, background jobs, mileage automation beyond current API behavior, or new schedule semantics.

**Deliverables:** Maintenance and schedule list/detail/form patterns.

**Acceptance Criteria:** Users can distinguish a record from a schedule; schedule controls expose only supported DATE, MILEAGE, and DATE_OR_MILEAGE behavior; UI never implies automatic work creation or reminders.

**Verification:** Shared verification; tests for schedule validation/empty/error states and maintenance completion; responsive review of dense vehicle-related content.

## Step 50 — Expense Screens Rebuild

### Must Read

Step 40 outputs, expense routes/pages/components, expense hooks/selectors, generated expense APIs, and financial user flows.

### Objectives

Make expense entry and review reliable for daily finance work.

**Scope:** Rebuild expense list, detail, create, and edit screens; clarify vehicle association, category, date, amount, description, and downstream dashboard/analytics context.

**Excluded:** New expense categories, accounting integrations, altered financial calculations, or new payment methods.

**Deliverables:** Consistent expense list/detail/form patterns.

**Acceptance Criteria:** Amount/date direction is legible in RTL; category and vehicle association are explicit; loading/zero/error states are distinct; edits/mutations reconcile visible financial data.

**Verification:** Shared verification; form/mutation tests and list mobile-card/table review.

## Step 51 — Task and Recurring Task Screens Rebuild

### Must Read

Step 40 outputs, task routes/pages/components, task hooks/selectors, generated task APIs, and task user flows.

### Objectives

Make operational task state and recurrence understandable without implying automation outside the contract.

**Scope:** Rebuild task list, detail, create, edit, and complete flows; expose recurrence values `NONE`, `DAILY`, `WEEKLY`, and `MONTHLY`; show predecessor/next-occurrence context when returned by the API.

**Excluded:** Scheduler, notifications, advanced/yearly/custom recurrence, arbitrary task associations, or client-side occurrence generation.

**Deliverables:** Task list/detail/form patterns with truthful recurrence copy.

**Acceptance Criteria:** Completion communicates the existing next-occurrence behavior; users can distinguish pending, completed, overdue, and recurring state; no UI promises a reminder or automated background processing.

**Verification:** Shared verification; tests for each recurrence value, completion, empty/error states, and RTL date handling.

---

## Step 52 — Analytics Screens Rebuild

### Must Read

Step 40 outputs, analytics page/tests, report/financial selectors and hooks, generated APIs, and dashboard period rules.

### Objectives

Present financial, vehicle, and maintenance insights without misleading periods or states.

**Scope:** Rebuild analytics layout; make period, source scope, and data freshness clear; provide mobile-readable chart/table alternatives; preserve current supported calculations.

**Excluded:** New analytics APIs, data warehouse work, new KPIs, predictive analytics, or accounting integration.

**Deliverables:** Responsive analytics screen patterns and accessible alternatives.

**Acceptance Criteria:** Every chart answers a business question and has a textual/tabular summary; zero, unavailable, loading, and error are distinct; numbers and periods remain legible in RTL.

**Verification:** Shared verification; chart/table state tests, screen-reader summary review, and all target widths.

## Step 53 — Reports Screens Rebuild

### Must Read

Step 40 outputs, reports page/tests, export/print implementation, report selectors/hooks, and contract flows.

### Objectives

Make reports clear, exportable, and separate from rental contract workflows.

**Scope:** Rebuild report selection/layout; clarify period and report scope; preserve existing printable/CSV export behavior where supported; add loading, empty, error, export-failure, and retry treatment.

**Excluded:** New report formats, reporting APIs, contract-template changes, accounting integrations, or new export infrastructure.

**Deliverables:** Report UI patterns and export/print feedback states.

**Acceptance Criteria:** Business reports are visibly distinct from rental contracts; export/print failures preserve context and provide recovery; selected period/scope is always visible.

**Verification:** Shared verification; tests for report/filter/export states and responsive print-preview/manual review where applicable.

---

## Step 54 — Auth Flow Completion

### Must Read

Step 40 outputs, `App.tsx`, `AuthProvider`, token/API configuration, login page/tests, generated auth APIs, and auth user flows.

### Objectives

Complete existing auth-capability UI and establish predictable frontend session behavior.

**Scope:** Polish login; add registration, password-reset request/confirmation, and invitation-acceptance routes using existing generated contracts; implement session expiry/refresh/retry behavior; provide safe Arabic validation and recovery messages.

**Excluded:** Real reset-email delivery, password policy changes, MFA/OAuth, backend token changes, or secure platform-owner bootstrap.

**Deliverables:** Public auth routes, protected-route/session behavior, and focused auth forms.

**Acceptance Criteria:** Public flows remain outside the tenant shell; expired access handling attempts only supported refresh behavior then clears session safely; failed auth never exposes tokens or technical details; redirects avoid loops.

**Verification:** Shared verification; tests for login, registration, reset request/confirm, invitation acceptance, expired/invalid refresh, protected-route redirect, and keyboard form behavior.

## Step 55 — Organization Profile and Account State

### Must Read

Step 40 outputs, shell/navigation, auth state, generated organization/user APIs, lifecycle rules in API design, and invitation contracts.

### Objectives

Surface owner-facing company/account workflows and truthful non-operational organization state.

**Scope:** Add organization profile screen for existing company fields; add appropriate owner invitation entry points and session actions; present `SUSPENDED`/`CANCELLED` explanation while retaining allowed account-status access; integrate account/company destination into navigation.

**Excluded:** Full platform-admin dashboard, tenant lifecycle controls, audit-log UI, billing, support tooling, or role model changes.

**Deliverables:** Organization profile/account pages and non-operational tenant state UI.

**Acceptance Criteria:** Owners see only authorized tenant actions; business work is clearly unavailable when the API returns non-operational state; employees do not see owner-only controls; platform controls remain outside normal tenant navigation.

**Verification:** Shared verification; role/permission tests, organization-profile mutation tests, suspended/cancelled API-state review, and responsive account navigation checks.

---

## Step 56 — Full Frontend Regression and Responsive QA

### Must Read

All completed Step 40-55 deliverables, current web test suite, generated client contracts, and the design-system verification checklist.

### Objectives

Establish that the tenant-facing frontend is ready for final human product QA and the Milestone 6 handoff.

**Scope:** Run complete web verification; validate affected API integrations; test responsive/RTL/accessibility behavior; review critical workflows: login/register, password reset, invitation acceptance, organization profile, customer, vehicle, rental, payment, contract, maintenance/schedule, expense, task/recurrence, analytics, and reports.

**Excluded:** Production deployment, offline synchronization, platform-admin dashboard, backend remediation, new features, or Milestone 6 operations.

**Deliverables:** Regression result, responsive/accessibility evidence, deferred-risk list, and updated milestone completion status.

**Acceptance Criteria:** All completed screens use one authoritative visible data source; no known P0 data-truthfulness, auth-flow, RTL, or accessibility blocker remains; all supported workflows preserve business/API behavior; deferred Milestone 6 work is explicitly recorded.

**Verification:** Run the full shared verification suite, generated-client/API checks where touched, manual review at all target widths, keyboard/screen-reader checks, and AI/product-owner review. Final human product QA occurs after this milestone, not before it.

## Milestone Acceptance Criteria

- The tenant-facing UI follows `12-ui-design-system.md` and resolves the P0/P1 audit findings without a frontend rewrite.
- Shared structure, shell, API state handling, and design-system patterns are in place before screen rebuilds.
- Dashboard, core business, operations, insights, auth, account, and organization workflows are responsive, Arabic-first, accessible, and API-consistent.
- Document expiry, maintenance schedules, recurring tasks, registration, reset, invitation acceptance, organization profile, and non-operational tenant state use already-approved backend capabilities only.
- Platform-only controls and all explicit non-goals remain out of tenant-facing UI scope.
- Every implementation step satisfies the Rule, shared verification, product review, and commit requirement.

## Completion Checklist

- [x] Step 40 - Frontend audit and design-system baseline completed
- [ ] Step 41 - Feature-based frontend structure plan approved
- [ ] Step 42 - Frontend Architecture Migration & Shared Infrastructure completed
- [ ] Step 43 - App shell and navigation completed
- [ ] Step 44 - Dashboard data consistency and period semantics completed
- [ ] Step 45 - Dashboard UI rebuild completed
- [ ] Step 46 - Vehicle screens rebuilt
- [ ] Step 47 - Customer screens rebuilt
- [ ] Step 48 - Rental, contract, and payment screens rebuilt
- [ ] Step 49 - Maintenance and maintenance schedule screens rebuilt
- [ ] Step 50 - Expense screens rebuilt
- [ ] Step 51 - Task and recurring task screens rebuilt
- [ ] Step 52 - Analytics screens rebuilt
- [ ] Step 53 - Reports screens rebuilt
- [ ] Step 54 - Auth flows completed
- [ ] Step 55 - Organization profile and account-state UX completed
- [ ] Step 56 - Full frontend regression and responsive QA completed
- [ ] Final human product QA completed after Milestone 5.8

## Milestone Status

**Status:** Planned

**Current Step:** Step 41 - Feature-Based Frontend Structure Plan

**Next Step:** Step 41 may begin after product-owner review of this milestone plan.
